# Pinned for reproducible, cache-stable builds (this is what stops update
# drift). Bump via Dependabot PRs for security, or alongside an Alpine base
# bump in build.yaml when the bundled Python version changes.
aiohttp==3.14.3
flask==3.1.3
waitress==3.0.2
# Octopus PDF-bill → CSV parsing (Data Management → build a CSV from bills). Pure
# Python, light. bill_parser imports it lazily/guarded, so a build without it just
# disables the "build from bills" feature — the rest of EMT is unaffected.
pypdf==6.16.1